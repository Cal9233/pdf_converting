# PDF to Excel Converter Requirements
# Note: This file is for reference only when using conda setup
# The setup scripts will install packages automatically

# Core packages (installed via conda)
pandas>=2.0.0
openpyxl>=3.1.0

# PDF processing (installed via pip)
pdfplumber>=0.10.0
pytesseract>=0.3.0

# OCR engine (installed via conda-forge)
# tesseract>=4.0.0

# Additional dependencies (automatically installed)
# Pillow>=8.0.0
# numpy>=1.20.0
# packaging>=21.0